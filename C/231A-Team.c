#include <stdio.h>

int main()
{
    short T, total = 0, a, b, c;
    scanf("%d", &T);
    while (T--)
    {
        while (scanf("%d", &a))
        {
            count = 0;
            scanf("%d", a);
            total += count;
        }
    }

    printf("%d", total);

    return 0;
}